#include <bits/stdc++.h>

using namespace std;
#define lli unsigned long long
#define SIZE 101

lli gcd(lli a, lli b)
{
    if (b == 0)
        return a;
    return gcd(b, a % b);

}
int main()
{lli a,b;
g=gcd(a,b);
for(int )
    return 0;
}
